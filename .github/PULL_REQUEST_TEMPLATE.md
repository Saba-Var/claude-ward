## What and why

<!-- One or two sentences: what does this PR change, and why. -->

## Tests added

New detection signatures require a fixture file under `test/fixtures/` and a corresponding unit test. Other changes should include tests where the logic is non-trivial.

## Checklist

- [ ] `npm test` passes
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] CHANGELOG.md updated under Unreleased
